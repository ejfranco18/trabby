<h3 class="mt-3 center mb-3 dark-blue">Your Travel Plans</h3>
<div class="container-plan mb-5">

  <% Plan.where(user_id: @user.id).each do |plan|%>
    <%# image = plan.city.image %>
    <div class="card-trip">
      <!-- <div class="banner_top" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<%#= image %>)"> -->
      <%= link_to plan_path(plan.id) do %>
        <div class="plan-banner d-flex justify-content-center align-items-center" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<%= plan.city.image %>)">
          <h3 id="city-name"><%= plan.city.name %></h3>
        </div>
        <!-- <img src="<%#= plan.city.image %>" /> -->
      <% end %>
      <div class="card-trip-infos">
        <div>
          <p>From: <%= plan.start_date.strftime("%d of %B") %> <br>to <%= plan.end_date.strftime("%d of %B") %></p>
          <p><%= (plan.end_date - plan.start_date).to_i + 1 %> Days</p>
        </div>
        <div class="d-flex">
          <% plan.category_ids.each do |catID| %>
            <div><a href="" target="_blank"><button class="button_class mt-2 mr-2" id="category-btn" ><%= Category.find(catID).name %></button></a></div>
          <% end %>
        </div>
        <!-- <div>
          <a href="" target="_blank"><button class="button_class mr-2" id="white" ><%#= City.find(plan.city_id).name %></button></a>
        </div> -->
        <!-- <h2 class="card-trip-pricing">£99.99</h2> -->
        <!-- <img src="https://kitt.lewagon.com/placeholder/users/krokrob" class="card-trip-user avatar-bordered" /> -->
      </div>
    </div>
  <% end %>
</div>

<!-- <div
  id="map"
  style="width: 100%;
  height: 600px;"
  data-markers="<%#= @markers.to_json %>"
  data-mapbox-api-key="<%#= ENV['MAPBOX_API_KEY'] %>"
></div> -->
